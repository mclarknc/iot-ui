query getchannelsByMonitor($monitorId: Int) {
  channelsByMonitor (monitorId: $monitorId){
    id
    name
    channelType {
      commonName
      units {
        name
        unitImperial
      }
    }
  }
}
