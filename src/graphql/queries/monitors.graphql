query getAllMonitors {
  allMonitors(activeOnly: true) {
    id
    name
    model {
      name
      model
    }
    firmware {
      binName
      version
    }
    company {
      name
    }
    status
    lastUpdate
  }
}
