query getchannel($id: Int) {
  channel (id: $id){
    id
    name
    channelType {
      commonName
      units {
        name
        unitImperial
      }
    }
    updateInterval
    offsetValue
    displayTemplateName
  }
}
